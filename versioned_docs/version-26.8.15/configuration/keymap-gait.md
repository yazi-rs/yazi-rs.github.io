---
sidebar_class_name: "hidden"
---

Where `[gait]` can be one of the following:

- `"fine"`: Vim-like `word` motion, move by words, punctuation, and spaces.
- `"lean"`: Emacs-like `word` motion, move by words and spaces.
- `"wide"`: Vim-like `WORD` motion, move by whitespace-delimited words.
